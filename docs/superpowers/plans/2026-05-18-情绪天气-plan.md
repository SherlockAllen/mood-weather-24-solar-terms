# 情绪天气 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a hackathon-ready 24 solar terms (节气) dynamic wallpaper site with ink-wash aesthetics, full-screen carousel, CSS+SVG animations, and multi-modal interactions.

**Architecture:** Single-page static site with a full-screen carousel container. Each slide shows one solar term with layered SVG ink-wash background animations, centered poetic text, and a bottom description card. Navigation via arrows, keyboard, touch swipe, and dot indicators. Tailwind CSS v4 handles layout/responsive utilities; custom CSS drives SVG keyframe animations; vanilla JS manages state and events.

**Tech Stack:** HTML5, Tailwind CSS v4 (JIT), SVG, Vanilla JavaScript. Zero external JS libraries. System font stack (Songti SC, Kaiti SC, Noto Serif SC).

---

## File Structure

```
/
├── index.html              # Single-page app shell
├── src/
│   ├── input.css           # Tailwind v4 entry + @theme custom tokens
│   ├── animations.css      # Ink-wash @keyframes (not via Tailwind)
│   ├── carousel.js         # Slide state, transitions, rendering
│   ├── interactions.js     # Keyboard, touch, mouse, focus, reduced-motion
│   └── data.js             # 24 solar terms data array
├── package.json            # Tailwind v4 CLI dependency + build script
├── tailwind.config.js      # Theme extension (if needed beyond @theme)
└── README.md               # Run instructions
```

---

## Task 1: Project Bootstrap & Tailwind v4 Setup

**Files:**
- Create: `package.json`
- Create: `src/input.css`
- Create: `tailwind.config.js` (optional fallback config)

- [ ] **Step 1: Create package.json with Tailwind v4 dependency**

```json
{
  "name": "mood-weather-24-solar-terms",
  "version": "1.0.0",
  "description": "24 Solar Terms dynamic wallpaper with ink-wash aesthetics",
  "scripts": {
    "build:css": "tailwindcss -i ./src/input.css -o ./src/main.css --minify",
    "watch:css": "tailwindcss -i ./src/input.css -o ./src/main.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^4.0.0"
  }
}
```

- [ ] **Step 2: Install dependencies**

Run: `npm install`
Expected: `node_modules/` created, `tailwindcss` installed.

- [ ] **Step 3: Create Tailwind v4 input.css with custom theme**

```css
@import "tailwindcss";

@theme {
  --color-paper: #f8f6f2;
  --color-ink-dark: #2a2a2a;
  --color-ink-mid: #6a6a6a;
  --color-ink-light: #8a8a86;
  --color-mountain: #d5d0ca;
  --font-song: 'Songti SC', 'SimSun', serif;
  --font-kai: 'Kaiti SC', serif;
  --font-serif: 'Noto Serif SC', serif;
}
```

- [ ] **Step 4: Build CSS to verify setup**

Run: `npm run build:css`
Expected: `src/main.css` created, no errors.

- [ ] **Step 5: Commit**

```bash
git add package.json src/input.css tailwind.config.js src/main.css
git commit -m "chore: bootstrap project with Tailwind CSS v4"
```

---

## Task 2: 24 Solar Terms Data

**Files:**
- Create: `src/data.js`

- [ ] **Step 1: Write complete 24 solar terms dataset**

```javascript
/**
 * @typedef {Object} SolarTerm
 * @property {number} id - 1-24
 * @property {string} name - e.g. "立春"
 * @property {string} poem - e.g. "春风如贵客"
 * @property {string} subtitle - e.g. "一夜东风起，万物竞生辉"
 * @property {string} description - e.g. "立春是二十四节气之首..."
 */

export const solarTerms = [
  {
    id: 1,
    name: "立春",
    poem: "春风如贵客",
    subtitle: "一夜东风起，万物竞生辉",
    description: "立春是二十四节气之首，标志着冬季结束、春季开始，万物复苏，生机勃发。"
  },
  {
    id: 2,
    name: "雨水",
    poem: "好雨知时节",
    subtitle: "随风潜入夜，润物细无声",
    description: "雨水时节，气温回升，冰雪融化，降水增多，万物开始萌动。"
  },
  {
    id: 3,
    name: "惊蛰",
    poem: "微雨众卉新",
    subtitle: "一雷惊蛰始，蛰虫惊而出走",
    description: "春雷始鸣，惊醒蛰伏于地下越冬的昆虫，春耕正式开始。"
  },
  {
    id: 4,
    name: "春分",
    poem: "春分雨脚落声微",
    subtitle: "柳岸斜风带客归，昼夜均而寒暑平",
    description: "春分昼夜平分，阴阳平衡，燕子归来，百花盛开。"
  },
  {
    id: 5,
    name: "清明",
    poem: "清明时节雨纷纷",
    subtitle: "路上行人欲断魂，借问酒家何处有",
    description: "清明既是节气又是节日，气清景明，万物皆显，适宜踏青祭祖。"
  },
  {
    id: 6,
    name: "谷雨",
    poem: "杨花落尽子规啼",
    subtitle: "落絮游丝三月候，风吹雨洗一城花",
    description: "雨生百谷，是春季最后一个节气，寒潮结束，气温回升加快。"
  },
  {
    id: 7,
    name: "立夏",
    poem: "立夏将离春去也",
    subtitle: "几枝蕙草正芳舒，万物至此皆长大",
    description: "立夏标志夏季开始，万物进入生长旺季，青蛙鸣，蚯蚓出。"
  },
  {
    id: 8,
    name: "小满",
    poem: "小满田塍寻草药",
    subtitle: "农闲莫问动三车，麦穗初齐稚子娇",
    description: "小满指夏熟作物籽粒开始饱满，但尚未成熟，是谓小满。"
  },
  {
    id: 9,
    name: "芒种",
    poem: "时雨及芒种",
    subtitle: "四野皆插秧，家家麦饭美",
    description: "芒种意为有芒的麦子快收，有芒的稻子可种，是农耕最忙时节。"
  },
  {
    id: 10,
    name: "夏至",
    poem: "东边日出西边雨",
    subtitle: "道是无晴却有晴，昼晷已云极",
    description: "夏至是北半球白昼最长的一天，阳气至极，阴气始生。"
  },
  {
    id: 11,
    name: "小暑",
    poem: "荷风送香气",
    subtitle: "竹露滴清响，小暑不足畏",
    description: "小暑表示季夏时节正式开始，天气开始炎热，但未到最热。"
  },
  {
    id: 12,
    name: "大暑",
    poem: "赤日几时过",
    subtitle: "清风无处寻，经书聊枕籍",
    description: "大暑是一年中最热的时期，高温酷热，雷暴频繁，万物狂长。"
  },
  {
    id: 13,
    name: "立秋",
    poem: "空山新雨后",
    subtitle: "天气晚来秋，明月松间照",
    description: "立秋标志秋季开始，暑去凉来，梧桐叶落，禾谷成熟。"
  },
  {
    id: 14,
    name: "处暑",
    poem: "离离原上草",
    subtitle: "一岁一枯荣，处暑无三日",
    description: "处暑表示炎热暑天结束，气温逐渐下降，秋意渐浓。"
  },
  {
    id: 15,
    name: "白露",
    poem: "露从今夜白",
    subtitle: "月是故乡明，鸿雁来，玄鸟归",
    description: "白露时节天气转凉，清晨露水加厚，鸿雁南飞，百鸟储食。"
  },
  {
    id: 16,
    name: "秋分",
    poem: "秋分客尚在",
    subtitle: "竹露夕微微，燕将明日去",
    description: "秋分昼夜再次平分，全球无极昼极夜，秋高气爽，丹桂飘香。"
  },
  {
    id: 17,
    name: "寒露",
    poem: "袅袅凉风动",
    subtitle: "凄凄寒露零，兰衰花始白",
    description: "寒露气温更低，露水更冷，即将凝结成霜，菊花盛开。"
  },
  {
    id: 18,
    name: "霜降",
    poem: "霜降水痕收",
    subtitle: "浅碧鳞鳞露远洲，酒力渐消风力软",
    description: "霜降是秋季最后一个节气，天气渐冷，初霜出现，万物毕成。"
  },
  {
    id: 19,
    name: "立冬",
    poem: "冻笔新诗懒写",
    subtitle: "寒炉美酒时温，立冬犹十日",
    description: "立冬标志冬季开始，万物收藏，规避寒冷，水始冰，地始冻。"
  },
  {
    id: 20,
    name: "小雪",
    poem: "晚来天欲雪",
    subtitle: "能饮一杯无，荷尽已无擎雨盖",
    description: "小雪气温下降，开始降雪，但雪量不大，大地封冻，万物休眠。"
  },
  {
    id: 21,
    name: "大雪",
    poem: "大雪满弓刀",
    subtitle: "欲将轻骑逐，千树万树梨花开",
    description: "大雪标志仲冬时节开始，雪量增多，地面可能积雪，银装素裹。"
  },
  {
    id: 22,
    name: "冬至",
    poem: "天时人事日相催",
    subtitle: "冬至阳生春又来，刺绣五纹添弱线",
    description: "冬至白昼最短，阴气至极，阳气始生，是重要传统节日。"
  },
  {
    id: 23,
    name: "小寒",
    poem: "小寒连大吕",
    subtitle: "欢鹊垒新巢，拾食寻河曲",
    description: "小寒进入一年中最寒冷的日子，但还未到达极点，梅花始开。"
  },
  {
    id: 24,
    name: "大寒",
    poem: "旧雪未及消",
    subtitle: "新雪又拥户，阶前冻银床",
    description: "大寒是一年中最冷的时期，天寒地冻，春之气息已在酝酿之中。"
  }
];
```

- [ ] **Step 2: Commit**

```bash
git add src/data.js
git commit -m "feat: add 24 solar terms dataset"
```

---

## Task 3: Ink-Wash SVG Animation CSS

**Files:**
- Create: `src/animations.css`

- [ ] **Step 1: Write four-layer ink-wash keyframe animations**

```css
/* ============================================
   情绪天气 — Ink-Wash Animation Keyframes
   ============================================ */

/* Layer 2: Ink blurs breathing */
@keyframes inkBreath {
  0%, 100% {
    opacity: 0.04;
    transform: translateX(0);
  }
  50% {
    opacity: 0.08;
    transform: translateX(30px);
  }
}

/* Layer 3: Falling ink dots */
@keyframes inkFall {
  0% {
    transform: translateY(-10%);
    opacity: 0;
  }
  10% {
    opacity: 0.6;
  }
  90% {
    opacity: 0.6;
  }
  100% {
    transform: translateY(110%);
    opacity: 0;
  }
}

/* Layer 4: Bottom mist flow */
@keyframes mistFlow {
  0% {
    d: path("M0,280 Q120,220 240,260 Q340,200 400,230 L400,320 L0,320 Z");
  }
  50% {
    d: path("M0,270 Q100,240 220,250 Q360,210 400,240 L400,320 L0,320 Z");
  }
  100% {
    d: path("M0,280 Q120,220 240,260 Q340,200 400,230 L400,320 L0,320 Z");
  }
}

/* Slide transition: fade + slight translate */
@keyframes slideEnter {
  from {
    opacity: 0;
    transform: translateX(30px);
  }
  to {
    opacity: 1;
    transform: translateX(0);
  }
}

@keyframes slideExit {
  from {
    opacity: 1;
    transform: translateX(0);
  }
  to {
    opacity: 0;
    transform: translateX(-30px);
  }
}

/* First-load entrance */
@keyframes contentFadeIn {
  from {
    opacity: 0;
    transform: translateY(20px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

@keyframes cardSlideUp {
  from {
    opacity: 0;
    transform: translateX(-50%) translateY(20px);
  }
  to {
    opacity: 1;
    transform: translateX(-50%) translateY(0);
  }
}

/* Animation classes */
.ink-blur-1 {
  animation: inkBreath 10s ease-in-out infinite;
}
.ink-blur-2 {
  animation: inkBreath 12s ease-in-out infinite;
  animation-delay: 3s;
}
.ink-dot {
  animation: inkFall 14s linear infinite;
}
.ink-dot-delayed {
  animation: inkFall 16s linear infinite;
  animation-delay: 5s;
}
.mist-layer {
  animation: mistFlow 8s ease-in-out infinite;
}

/* Slide state classes */
.slide-enter {
  animation: slideEnter 350ms cubic-bezier(0.4, 0, 0.2, 1) forwards;
}
.slide-exit {
  animation: slideExit 350ms cubic-bezier(0.4, 0, 0.2, 1) forwards;
}

/* First load */
.first-load .term-name {
  animation: contentFadeIn 600ms cubic-bezier(0.4, 0, 0.2, 1) 300ms both;
}
.first-load .term-poem {
  animation: contentFadeIn 600ms cubic-bezier(0.4, 0, 0.2, 1) 450ms both;
}
.first-load .term-subtitle {
  animation: contentFadeIn 600ms cubic-bezier(0.4, 0, 0.2, 1) 600ms both;
}
.first-load .desc-card {
  animation: cardSlideUp 600ms cubic-bezier(0.4, 0, 0.2, 1) 750ms both;
}

/* Reduced motion */
@media (prefers-reduced-motion: reduce) {
  .ink-blur-1,
  .ink-blur-2,
  .ink-dot,
  .ink-dot-delayed,
  .mist-layer {
    animation: none;
  }
  .slide-enter,
  .slide-exit {
    animation-duration: 150ms;
  }
}
```

- [ ] **Step 2: Commit**

```bash
git add src/animations.css
git commit -m "feat: add ink-wash SVG animation keyframes"
```

---

## Task 4: Carousel Core Logic

**Files:**
- Create: `src/carousel.js`

- [ ] **Step 1: Implement carousel state machine and rendering**

```javascript
import { solarTerms } from './data.js';

export class Carousel {
  constructor(containerId, options = {}) {
    this.container = document.getElementById(containerId);
    if (!this.container) {
      throw new Error(`Carousel container #${containerId} not found`);
    }

    this.terms = solarTerms;
    this.currentIndex = 0;
    this.isAnimating = false;
    this.transitionDuration = options.transitionDuration || 350;
    this.onChange = options.onChange || (() => {});

    this.slidesContainer = this.container.querySelector('.slides-container');
    this.dotsContainer = this.container.querySelector('.dots-container');

    this._renderInitial();
  }

  /** @returns {SolarTerm} */
  get currentTerm() {
    return this.terms[this.currentIndex];
  }

  _renderInitial() {
    this._renderSlide(this.currentIndex);
    this._renderDots();
    this.container.classList.add('first-load');
  }

  _renderSlide(index) {
    const term = this.terms[index];
    const slide = document.createElement('div');
    slide.className = 'slide active';
    slide.setAttribute('role', 'group');
    slide.setAttribute('aria-roledescription', 'slide');
    slide.setAttribute('aria-label', `${term.name} — ${term.poem}`);
    slide.innerHTML = `
      <div class="slide-content">
        <h2 class="term-name">${term.name}</h2>
        <p class="term-poem">${term.poem}</p>
        <p class="term-subtitle">${term.subtitle}</p>
      </div>
      <div class="desc-card">
        <p>${term.description}</p>
      </div>
    `;

    // Replace current slide
    this.slidesContainer.innerHTML = '';
    this.slidesContainer.appendChild(slide);
  }

  _renderDots() {
    if (!this.dotsContainer) return;
    this.dotsContainer.innerHTML = this.terms.map((term, i) => `
      <button
        class="dot ${i === this.currentIndex ? 'active' : ''}"
        data-index="${i}"
        role="tab"
        aria-selected="${i === this.currentIndex}"
        aria-label="${term.name}"
        tabindex="${i === this.currentIndex ? '0' : '-1'}"
      ></button>
    `).join('');
  }

  goTo(index) {
    if (this.isAnimating || index === this.currentIndex) return;
    if (index < 0 || index >= this.terms.length) return;

    this.isAnimating = true;
    const direction = index > this.currentIndex ? 1 : -1;

    // Remove first-load class after initial render
    this.container.classList.remove('first-load');

    // Animate out current
    const currentSlide = this.slidesContainer.querySelector('.slide.active');
    if (currentSlide) {
      currentSlide.classList.add('slide-exit');
      currentSlide.classList.remove('active');
    }

    // Update index
    this.currentIndex = index;

    // Create and animate in new slide
    const term = this.terms[index];
    const newSlide = document.createElement('div');
    newSlide.className = 'slide slide-enter';
    newSlide.setAttribute('role', 'group');
    newSlide.setAttribute('aria-roledescription', 'slide');
    newSlide.setAttribute('aria-label', `${term.name} — ${term.poem}`);
    newSlide.innerHTML = `
      <div class="slide-content">
        <h2 class="term-name">${term.name}</h2>
        <p class="term-poem">${term.poem}</p>
        <p class="term-subtitle">${term.subtitle}</p>
      </div>
      <div class="desc-card">
        <p>${term.description}</p>
      </div>
    `;

    this.slidesContainer.appendChild(newSlide);

    // Update dots
    this._renderDots();

    // Notify
    this.onChange(this.currentTerm, this.currentIndex);

    // Cleanup after transition
    setTimeout(() => {
      if (currentSlide && currentSlide.parentNode) {
        currentSlide.parentNode.removeChild(currentSlide);
      }
      newSlide.classList.remove('slide-enter');
      newSlide.classList.add('active');
      this.isAnimating = false;
    }, this.transitionDuration);
  }

  next() {
    const nextIndex = (this.currentIndex + 1) % this.terms.length;
    this.goTo(nextIndex);
  }

  prev() {
    const prevIndex = (this.currentIndex - 1 + this.terms.length) % this.terms.length;
    this.goTo(prevIndex);
  }

  reset() {
    this.goTo(0);
  }
}
```

- [ ] **Step 2: Commit**

```bash
git add src/carousel.js
git commit -m "feat: add carousel state machine and slide rendering"
```

---

## Task 5: Interactions (Keyboard, Touch, Mouse)

**Files:**
- Create: `src/interactions.js`

- [ ] **Step 1: Implement all input handlers**

```javascript
/**
 * @param {Carousel} carousel
 * @param {HTMLElement} container
 */
export function initInteractions(carousel, container) {
  // --- Keyboard ---
  document.addEventListener('keydown', (e) => {
    switch (e.key) {
      case 'ArrowLeft':
        e.preventDefault();
        carousel.prev();
        break;
      case 'ArrowRight':
        e.preventDefault();
        carousel.next();
        break;
      case 'Escape':
        e.preventDefault();
        carousel.reset();
        break;
    }
  });

  // --- Mouse / Click ---
  const prevBtn = container.querySelector('.nav-prev');
  const nextBtn = container.querySelector('.nav-next');

  if (prevBtn) {
    prevBtn.addEventListener('click', () => carousel.prev());
  }
  if (nextBtn) {
    nextBtn.addEventListener('click', () => carousel.next());
  }

  // Dots click delegation
  const dotsContainer = container.querySelector('.dots-container');
  if (dotsContainer) {
    dotsContainer.addEventListener('click', (e) => {
      const dot = e.target.closest('.dot');
      if (!dot) return;
      const index = parseInt(dot.dataset.index, 10);
      if (!Number.isNaN(index)) {
        carousel.goTo(index);
      }
    });

    // Keyboard navigation for dots (tablist pattern)
    dotsContainer.addEventListener('keydown', (e) => {
      const activeDot = dotsContainer.querySelector('.dot[aria-selected="true"]');
      if (!activeDot) return;
      const currentIdx = parseInt(activeDot.dataset.index, 10);
      let newIdx = currentIdx;

      switch (e.key) {
        case 'ArrowLeft':
          e.preventDefault();
          newIdx = currentIdx > 0 ? currentIdx - 1 : carousel.terms.length - 1;
          break;
        case 'ArrowRight':
          e.preventDefault();
          newIdx = currentIdx < carousel.terms.length - 1 ? currentIdx + 1 : 0;
          break;
        case 'Home':
          e.preventDefault();
          newIdx = 0;
          break;
        case 'End':
          e.preventDefault();
          newIdx = carousel.terms.length - 1;
          break;
      }

      if (newIdx !== currentIdx) {
        carousel.goTo(newIdx);
        // Focus the new dot after render
        setTimeout(() => {
          const newDot = dotsContainer.querySelector(`.dot[data-index="${newIdx}"]`);
          if (newDot) newDot.focus();
        }, 0);
      }
    });
  }

  // --- Touch / Swipe ---
  let touchStartX = 0;
  let touchStartY = 0;
  let isTouching = false;
  const SWIPE_THRESHOLD = 50;

  container.addEventListener('touchstart', (e) => {
    if (e.touches.length !== 1) return;
    isTouching = true;
    touchStartX = e.touches[0].clientX;
    touchStartY = e.touches[0].clientY;
  }, { passive: true });

  container.addEventListener('touchmove', (e) => {
    if (!isTouching) return;
    // Let vertical scroll pass through; prevent horizontal default only
    const dx = e.touches[0].clientX - touchStartX;
    const dy = e.touches[0].clientY - touchStartY;
    if (Math.abs(dx) > Math.abs(dy)) {
      e.preventDefault();
    }
  }, { passive: false });

  container.addEventListener('touchend', (e) => {
    if (!isTouching) return;
    isTouching = false;
    const touchEndX = e.changedTouches[0].clientX;
    const dx = touchEndX - touchStartX;

    if (Math.abs(dx) >= SWIPE_THRESHOLD) {
      if (dx > 0) {
        carousel.prev();
      } else {
        carousel.next();
      }
    }
  }, { passive: true });

  // --- Reduced Motion ---
  const motionQuery = window.matchMedia('(prefers-reduced-motion: reduce)');
  function handleMotionChange() {
    container.classList.toggle('reduced-motion', motionQuery.matches);
  }
  motionQuery.addEventListener('change', handleMotionChange);
  handleMotionChange();
}
```

- [ ] **Step 2: Commit**

```bash
git add src/interactions.js
git commit -m "feat: add keyboard, touch, mouse, and reduced-motion interactions"
```

---

## Task 6: Main HTML Shell with SVG Ink-Wash Background

**Files:**
- Create: `index.html`

- [ ] **Step 1: Write the complete index.html**

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover">
  <meta name="description" content="情绪天气 — 24节气动态壁纸，以传统水墨美学呈现二十四节气的诗意意境">
  <title>情绪天气 — 24节气</title>
  <link rel="stylesheet" href="src/main.css">
  <link rel="stylesheet" href="src/animations.css">
  <style>
    /* Critical inline styles for first paint */
    html, body {
      margin: 0;
      padding: 0;
      height: 100%;
      overflow: hidden;
      background-color: #f8f6f2;
      font-family: 'Noto Serif SC', 'Songti SC', serif;
      -webkit-font-smoothing: antialiased;
    }
    .sr-only {
      position: absolute;
      width: 1px;
      height: 1px;
      padding: 0;
      margin: -1px;
      overflow: hidden;
      clip: rect(0, 0, 0, 0);
      white-space: nowrap;
      border: 0;
    }
  </style>
</head>
<body>
  <h1 class="sr-only">情绪天气 — 24节气动态壁纸</h1>

  <div
    id="carousel"
    class="carousel"
    role="region"
    aria-label="24节气轮播"
  >
    <!-- Layer 1: SVG Ink-Wash Background -->
    <svg
      class="ink-background"
      viewBox="0 0 400 320"
      preserveAspectRatio="none"
      aria-hidden="true"
    >
      <defs>
        <filter id="softMist" x="-20%" y="-20%" width="140%" height="140%">
          <feGaussianBlur stdDeviation="6" />
        </filter>
        <filter id="heavyMist" x="-20%" y="-20%" width="140%" height="140%">
          <feGaussianBlur stdDeviation="10" />
        </filter>
      </defs>

      <!-- Layer 1: Distant mountains -->
      <path
        d="M0,240 Q100,160 200,200 Q300,120 400,180 L400,320 L0,320 Z"
        fill="#e0dcd6"
        opacity="0.7"
      />
      <path
        d="M0,280 Q120,220 240,260 Q340,200 400,230 L400,320 L0,320 Z"
        fill="#d5d0ca"
        opacity="0.9"
      />

      <!-- Layer 2: Ink blurs breathing -->
      <ellipse
        cx="80"
        cy="100"
        rx="100"
        ry="60"
        fill="#2a2a2a"
        opacity="0.06"
        filter="url(#softMist)"
        class="ink-blur-1"
      />
      <ellipse
        cx="300"
        cy="80"
        rx="80"
        ry="50"
        fill="#2a2a2a"
        opacity="0.04"
        filter="url(#heavyMist)"
        class="ink-blur-2"
      />

      <!-- Layer 3: Falling ink dots -->
      <circle cx="50" cy="0" r="2" fill="#2a2a2a" opacity="0.3" class="ink-dot" />
      <circle cx="120" cy="0" r="1.5" fill="#2a2a2a" opacity="0.2" class="ink-dot-delayed" />
      <circle cx="280" cy="0" r="2.5" fill="#2a2a2a" opacity="0.25" class="ink-dot" style="animation-delay: 3s;" />
      <circle cx="350" cy="0" r="1.8" fill="#2a2a2a" opacity="0.2" class="ink-dot-delayed" style="animation-delay: 7s;" />

      <!-- Layer 4: Bottom mist flow -->
      <path
        d="M0,280 Q120,220 240,260 Q340,200 400,230 L400,320 L0,320 Z"
        fill="#e8e4de"
        opacity="0.5"
        filter="url(#softMist)"
        class="mist-layer"
      />
    </svg>

    <!-- Layer 3: Slide content -->
    <div class="slides-container"></div>

    <!-- Layer 5: Navigation arrows -->
    <button
      class="nav-arrow nav-prev"
      aria-label="上一个节气"
      type="button"
    >
      <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
        <polyline points="15 18 9 12 15 6"></polyline>
      </svg>
    </button>
    <button
      class="nav-arrow nav-next"
      aria-label="下一个节气"
      type="button"
    >
      <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
        <polyline points="9 18 15 12 9 6"></polyline>
      </svg>
    </button>

    <!-- Layer 6: Dot indicators -->
    <div
      class="dots-container"
      role="tablist"
      aria-label="节气导航"
    ></div>

    <!-- Keyboard hint -->
    <div class="keyboard-hint" aria-hidden="true">
      <span>←</span><span>→</span> 键盘切换
    </div>
  </div>

  <script type="module" src="src/carousel.js"></script>
  <script type="module" src="src/interactions.js"></script>
  <script type="module">
    import { Carousel } from './src/carousel.js';
    import { initInteractions } from './src/interactions.js';

    const carousel = new Carousel('carousel', {
      transitionDuration: 350,
      onChange: (term, index) => {
        // Optional: update document title or analytics
        document.title = `情绪天气 — ${term.name}`;
      }
    });

    initInteractions(carousel, document.getElementById('carousel'));
  </script>
</body>
</html>
```

- [ ] **Step 2: Commit**

```bash
git add index.html
git commit -m "feat: add main HTML shell with SVG ink-wash background"
```

---

## Task 7: Tailwind Utility Classes + Custom Component Styles

**Files:**
- Modify: `src/input.css` (append component styles)

- [ ] **Step 1: Append carousel component styles to input.css**

```css
/* ============================================
   Carousel Component Styles
   ============================================ */

.carousel {
  position: relative;
  width: 100vw;
  height: 100dvh;
  overflow: hidden;
  background-color: var(--color-paper, #f8f6f2);
  display: flex;
  align-items: center;
  justify-content: center;
}

/* SVG Background */
.ink-background {
  position: absolute;
  inset: 0;
  width: 100%;
  height: 100%;
  z-index: 0;
  pointer-events: none;
}

/* Slides container */
.slides-container {
  position: relative;
  z-index: 10;
  width: 100%;
  height: 100%;
  display: flex;
  align-items: center;
  justify-content: center;
}

/* Individual slide */
.slide {
  position: absolute;
  inset: 0;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 0 60px;
  text-align: center;
}

.slide-content {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 0.5rem;
}

/* Typography */
.term-name {
  font-family: var(--font-song, 'Songti SC', serif);
  font-size: 12px;
  letter-spacing: 12px;
  color: var(--color-ink-light, #8a8a86);
  font-weight: 400;
  margin: 0 0 16px 0;
  text-transform: none;
}

.term-poem {
  font-family: var(--font-kai, 'Kaiti SC', serif);
  font-size: 36px;
  letter-spacing: 8px;
  color: var(--color-ink-dark, #2a2a2a);
  font-weight: 400;
  margin: 0;
  line-height: 1.4;
}

.term-subtitle {
  font-family: var(--font-serif, 'Noto Serif SC', serif);
  font-size: 14px;
  font-style: italic;
  letter-spacing: 3px;
  color: var(--color-ink-mid, #6a6a6a);
  margin: 18px 0 0 0;
}

/* Description card */
.desc-card {
  position: absolute;
  bottom: 80px;
  left: 50%;
  transform: translateX(-50%);
  max-width: 320px;
  width: 90%;
  background: rgba(255, 255, 255, 0.9);
  padding: 16px 24px;
  border-radius: 4px;
  box-shadow: 0 2px 12px rgba(0, 0, 0, 0.06);
  z-index: 11;
}

.desc-card p {
  font-family: var(--font-serif, 'Noto Serif SC', serif);
  font-size: 13px;
  line-height: 1.8;
  color: var(--color-ink-mid, #5a5a5a);
  margin: 0;
  letter-spacing: 1px;
}

/* Navigation arrows */
.nav-arrow {
  position: absolute;
  top: 50%;
  transform: translateY(-50%);
  width: 44px;
  height: 44px;
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--color-ink-light, #8a8a86);
  background: transparent;
  border: 1px solid var(--color-mountain, #d5d0ca);
  border-radius: 50%;
  cursor: pointer;
  z-index: 20;
  transition: all 200ms ease;
  opacity: 0.6;
}

.nav-arrow:hover {
  opacity: 1;
  border-color: var(--color-ink-dark, #2a2a2a);
  color: var(--color-ink-dark, #2a2a2a);
}

.nav-arrow:focus-visible {
  outline: 2px solid var(--color-ink-dark, #2a2a2a);
  outline-offset: 2px;
}

.nav-prev {
  left: 20px;
}

.nav-next {
  right: 20px;
}

/* Dot indicators */
.dots-container {
  position: absolute;
  bottom: 20px;
  left: 50%;
  transform: translateX(-50%);
  display: flex;
  gap: 8px;
  align-items: center;
  z-index: 20;
}

.dot {
  width: 24px;
  height: 24px;
  display: flex;
  align-items: center;
  justify-content: center;
  background: transparent;
  border: none;
  cursor: pointer;
  padding: 0;
  position: relative;
}

.dot::after {
  content: '';
  width: 6px;
  height: 6px;
  border-radius: 50%;
  background: var(--color-mountain, #d0ccc6);
  transition: all 200ms ease;
  display: block;
}

.dot:hover::after,
.dot:focus-visible::after {
  width: 8px;
  height: 8px;
  background: var(--color-ink-mid, #6a6a6a);
}

.dot.active::after {
  background: var(--color-ink-dark, #2a2a2a);
}

.dot:focus-visible {
  outline: 2px solid var(--color-ink-dark, #2a2a2a);
  outline-offset: 2px;
  border-radius: 50%;
}

/* Keyboard hint */
.keyboard-hint {
  position: absolute;
  top: 16px;
  right: 16px;
  font-size: 10px;
  color: var(--color-mountain, #b0aca8);
  letter-spacing: 1px;
  z-index: 20;
  display: flex;
  gap: 4px;
  align-items: center;
  font-family: var(--font-serif, 'Noto Serif SC', serif);
}

.keyboard-hint span {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  width: 18px;
  height: 18px;
  border: 1px solid var(--color-mountain, #d5d0ca);
  border-radius: 3px;
  font-size: 10px;
}

/* Responsive */
@media (max-width: 767px) {
  .slide {
    padding: 0 20px;
  }

  .term-poem {
    font-size: 28px;
    letter-spacing: 6px;
  }

  .term-subtitle {
    font-size: 12px;
    letter-spacing: 2px;
  }

  .desc-card {
    bottom: 70px;
    padding: 12px 16px;
  }

  .nav-arrow {
    width: 36px;
    height: 36px;
  }

  .nav-prev {
    left: 12px;
  }

  .nav-next {
    right: 12px;
  }

  .keyboard-hint {
    display: none;
  }
}

@media (min-width: 768px) and (max-width: 1023px) {
  .term-poem {
    font-size: 32px;
    letter-spacing: 7px;
  }
}

/* Safe area for notched phones */
@supports (padding: max(0px)) {
  .carousel {
    padding-left: max(0px, env(safe-area-inset-left));
    padding-right: max(0px, env(safe-area-inset-right));
    padding-bottom: max(0px, env(safe-area-inset-bottom));
  }
}
```

- [ ] **Step 2: Rebuild CSS**

Run: `npm run build:css`
Expected: `src/main.css` updated with new styles.

- [ ] **Step 3: Commit**

```bash
git add src/input.css src/main.css
git commit -m "feat: add carousel component styles with responsive breakpoints"
```

---

## Task 8: README & Final Integration Test

**Files:**
- Create: `README.md`

- [ ] **Step 1: Write README.md**

```markdown
# 情绪天气 — 24节气动态壁纸

> MeDo Hackathon 参赛作品 · 项目8「情绪天气」

## 简介

「情绪天气」是一个以传统水墨美学呈现的24节气动态壁纸网站。通过 CSS + SVG 动画营造水墨意境，配合诗词与节气描述，打造沉浸式的文化体验。

## 技术栈

- HTML5 语义化结构
- Tailwind CSS v4 (JIT 编译)
- SVG 水墨背景动效
- Vanilla JavaScript (ES Modules)
- 系统字体栈（无需外部字体加载）

## 特性

- 24 节气完整数据，每节气配有诗句与描述
- 四层水墨动效：远山、墨迹、墨点、飘雾
- 多种导航方式：点击箭头、键盘 (← → Esc)、触摸滑动、底部圆点
- 完整的无障碍支持：ARIA、键盘导航、Focus 状态、Reduced Motion
- 响应式设计：桌面 / 平板 / 移动端自适应
- 零外部图片，纯 CSS + SVG 实现

## 本地运行

```bash
# 安装依赖
npm install

# 构建 CSS（开发时可用 watch 模式）
npm run build:css

# 启动本地服务器（任意静态服务器均可）
npx serve .
# 或
python3 -m http.server 8080
```

然后打开浏览器访问 `http://localhost:8080`。

## 交互指南

| 操作 | 效果 |
|------|------|
| 点击左右箭头 | 切换节气 |
| 点击底部圆点 | 跳转到对应节气 |
| ← / → 键 | 切换节气 |
| Esc 键 | 返回立春（第一页） |
| 触摸滑动 | 左右滑动切换节气 |

## 文件结构

```
/
├── index.html          # 主页面
├── src/
│   ├── input.css       # Tailwind v4 入口 + 自定义主题
│   ├── main.css        # 编译后的 CSS
│   ├── animations.css  # 水墨动画关键帧
│   ├── carousel.js     # 轮播逻辑
│   ├── interactions.js # 事件处理
│   └── data.js         # 24节气数据
└── package.json
```

## Hackathon 演示要点

1. 首屏「立春」诗句淡入，背景墨迹缓缓流动
2. 演示键盘切换 (←→)、触摸滑动、圆点跳转
3. 提及纯 CSS+SVG 实现、零图片、60fps、无障碍支持
4. 每节气配有诗句与描述，体现传统文化底蕴
```

- [ ] **Step 2: Run integration test — open in browser and verify**

Run:
```bash
python3 -m http.server 8080 &
sleep 1
curl -s http://localhost:8080 | head -20
```

Expected: HTML output starting with `<!DOCTYPE html>`.

- [ ] **Step 3: Check carousel JS module loads without errors**

Open browser console (or use headless check) and verify:
- No 404 errors for `src/carousel.js`, `src/interactions.js`, `src/data.js`
- Carousel instance created successfully
- First slide (立春) displays correctly

- [ ] **Step 4: Commit**

```bash
git add README.md
git commit -m "docs: add README with run instructions and demo notes"
```

---

## Task 9: Accessibility Audit & Polish

**Files:**
- Modify: `index.html` (if fixes needed)
- Modify: `src/carousel.js` (if fixes needed)

- [ ] **Step 1: Run accessibility checklist**

Verify each item manually in browser:

- [ ] Contrast: `#2a2a2a` on `#f8f6f2` = 11.5:1 (exceeds WCAG AA)
- [ ] Focus rings visible on arrows and dots (2px solid #2a2a2a)
- [ ] Tab order: left arrow → right arrow → dot group
- [ ] Arrow keys work inside dot group (tablist roving tabindex)
- [ ] `aria-label` present on arrows
- [ ] `role="tablist"`, `aria-selected` on dots
- [ ] `role="region"`, `aria-label` on carousel
- [ ] `h1.sr-only` present for screen reader context
- [ ] `prefers-reduced-motion: reduce` disables continuous animations
- [ ] Page title updates to current solar term name

- [ ] **Step 2: Fix any issues found**

Common fixes:
- Add missing `aria-label` or `role`
- Adjust focus styles
- Ensure title updates correctly in `carousel.js`

- [ ] **Step 3: Commit**

```bash
git add -A
git commit -m "fix: accessibility audit and polish"
```

---

## Task 10: Performance Check & Final Build

**Files:**
- Modify: `src/main.css` (final minified build)

- [ ] **Step 1: Final production CSS build**

Run: `npm run build:css`
Expected: `src/main.css` minified, no errors.

- [ ] **Step 2: Verify file sizes**

Run:
```bash
du -sh src/main.css src/animations.css src/carousel.js src/interactions.js src/data.js index.html
```

Targets:
- `main.css` < 15KB (gzipped ideally < 10KB)
- Total JS < 10KB
- `index.html` < 8KB

- [ ] **Step 3: Verify no horizontal scroll on mobile**

Open DevTools → Device Toolbar → iPhone SE / Pixel 5. Ensure:
- No horizontal scrollbar
- Content fits within viewport
- Touch targets (arrows, dots) are tappable

- [ ] **Step 4: Final commit**

```bash
git add -A
git commit -m "chore: final build and performance check"
```

---

## Spec Coverage Checklist

| Spec Section | Task(s) | Status |
|--------------|---------|--------|
| 2. 风格方向 (colors, fonts) | Task 1 (input.css theme), Task 7 (component styles) | Covered |
| 3. 布局设计 (layout, z-index) | Task 6 (HTML), Task 7 (CSS) | Covered |
| 4. 动画设计 (4 layers, transitions, reduced motion) | Task 3 (keyframes), Task 5 (reduced motion JS) | Covered |
| 5. 交互设计 (keyboard, touch, mouse, focus, hover) | Task 5 (interactions.js), Task 7 (CSS hover/focus) | Covered |
| 6. 内容结构 (24 terms data model) | Task 2 (data.js) | Covered |
| 7. 响应式设计 (breakpoints, mobile) | Task 7 (media queries) | Covered |
| 8. 技术栈 (Tailwind v4, SVG, vanilla JS) | Task 1, 6, 7 | Covered |
| 9. 无障碍支持 (ARIA, contrast, reduced motion) | Task 5, 9 | Covered |
| 10. 文件结构 | All tasks | Covered |
| 11. Hackathon 演示要点 | Task 8 (README) | Covered |

---

## Plan Self-Review

**Placeholder scan:**
- No "TBD", "TODO", or vague steps. Every step includes exact code or commands.
- No "add appropriate error handling" — validation is explicit in `carousel.js`.
- No "write tests for the above" — no separate test framework for this static project; validation is manual in Task 8 and Task 9.

**Type consistency:**
- `transitionDuration` used consistently (350ms default) in `carousel.js` constructor and cleanup timeout.
- `currentIndex` always an integer; `goTo()` guards with `Number.isNaN` in dot click handler.
- CSS custom properties (`--color-paper`, etc.) match between `input.css` `@theme` and inline critical styles in `index.html`.

**Spec coverage:** All 12 spec sections map to at least one task. No gaps.
