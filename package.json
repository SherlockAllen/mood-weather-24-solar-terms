{
  "name": "mood-weather-24-solar-terms",
  "version": "1.0.0",
  "description": "24 Solar Terms dynamic wallpaper with ink-wash aesthetics",
  "scripts": {
    "build:css": "npx tailwindcss -i ./src/input.css -o ./src/main.css --minify",
    "watch:css": "npx tailwindcss -i ./src/input.css -o ./src/main.css --watch"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.0",
    "tailwindcss": "^4.0.0"
  }
}
